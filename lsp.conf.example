# LSP Configuration for texteditor
# Format: extensions:command[:display_name]
#
# extensions: comma-separated list of file extensions (with or without leading dot)
# command: the LSP server command to run
# display_name: optional display name for the server
#
# The config file is searched in:
#   1. ./lsp.conf (current directory)
#   2. $XDG_CONFIG_HOME/texteditor/lsp.conf
#   3. ~/.config/texteditor/lsp.conf
#
# If no config file is found, clangd is used by default for C/C++ files.

# C/C++ - clangd
.c,.h,.cpp,.cc,.cxx,.hpp,.hxx,.C:clangd --log=error:C/C++ (clangd)

# Python - pylsp or pyright
# .py:pylsp:Python (pylsp)
# .py:pyright-langserver --stdio:Python (pyright)

# Rust - rust-analyzer
# .rs:rust-analyzer:Rust

# Go - gopls
# .go:gopls:Go

# JavaScript/TypeScript - typescript-language-server
# .js,.jsx,.ts,.tsx:typescript-language-server --stdio:TypeScript

# JSON - vscode-json-languageserver
# .json:vscode-json-languageserver --stdio:JSON

# YAML - yaml-language-server
# .yml,.yaml:yaml-language-server --stdio:YAML

# Bash - bash-language-server
# .sh,.bash:bash-language-server start:Bash

# Lua - lua-language-server
# .lua:lua-language-server:Lua

# Zig - zls
# .zig:zls:Zig
